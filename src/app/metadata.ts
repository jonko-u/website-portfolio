import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: "Jon portfolio",
    description: 'This website shows the applied capabilities and the magnificent co-working',
  }